<!-- 定位：C++ 与资源整理结果、验证边界及后续维护清单。 -->

# 2026-09-09 代码与资源整理

## 已整理

- 保留已有 `hud/`、`questlist/`、`settings/`、`ui/` 拆分，补齐两处编译依赖：普通标记显式包含 NND 名称接口；设置加载从同目录包含私有头文件。
- 补齐此前只留下公共接口的 Hook 拆分：安装、任务匹配、普通标记、效果转发和第三方兼容分别放入 `source/hooks/`。内部声明及重定位放在私有 `Internal.h`，公开 `include/Hooks.h` 保持精简。
- Compass 的 11 个 GFx 方法从头文件移入 `source/ui/Compass.cpp`；公开头文件保留类型与接口。10 个 Hook 回调、2 个安装函数及 11 个 Compass 方法与冻结基线的函数体逐一比较一致（忽略空白）。
- 资源分成 `assets/main/`、`localization/zh-CN/`、`source/questlist/` 和 `generated/`；脚本留在 `tools/`，AS2/FLA 留在 `swf/`。中文配置使用覆盖包自己的 `config.json`，不再混入主包输入。
- 构建、嵌入、翻译和校验路径同步更新；新增 `tools/verify_package.py` 校验主包/中文包暂存目录与 ZIP 的文件集合和哈希。
- CMake 纳入 `source/` 私有头文件，修复两行损坏的 UTF-8 注释；修正仍读取已删除 Settings.cpp 的测试。BAT 保持 CRLF，避免 CMD 错误解析行边界。
- 中英文 README、架构、工具、素材及开发指南同步更新，新增 [C++ 导航](../../source/README.md) 与 [资源说明](../../assets/README.md)。冗长的旧 CONTEXT 原文迁入 [历史快照](context-history-20260909.md)，L1 只保留现状与导航。

## 验证与交付边界

- DLL 原生编译、链接及 `build.bat` 完整打包通过；保留既有 SE/AE 宏覆盖警告。
- Python 回归 23/23、正式 SWF 反编译布局/状态模拟 45/45、罗盘缩放/箭头/边界检查通过。
- 三份 SWF 哈希清单通过；移动的 8 个非文档资源字节不变，包括原始 SWF、SVG、中文配置及重新构建的中间 SWF。两份发布 Compass 也与整理前逐字节一致。
-  主包 13 个文件、CHS 1 个文件，暂存目录和 ZIP 分别与资源、当前 DLL、LICENSE 一一匹配；没有独立 QuestItemList.swf，也没有素材源或构建中间件泄漏。
- 双语配置与 C++ 绑定校验通过。新包未部署 MO2，未运行 VR；游戏加载、提前省略和调试红框仍需实机验收。Git 未暂存、未提交，原有暂存项保留。

## 后续值得整理

| 项目 | 原因与建议 |
|---|---|
| 构建脚本与本机配置 | `build.bat` 仍兼任工具路径配置和打包。后续可把本机路径迁到忽略的本地配置，将复杂清理/打包逻辑移入 PowerShell，BAT 保留启动与 VS 环境准备；需覆盖 clean、nobuild、无 7-Zip 和无 CHS 分支。 |
| GFx 包装器 | `include/IUI/` 与 `include/utils/` 存在同名包装器；当前调用主要使用 IUI 版本，接口实现并非全部相同。合并前应逐接口比较与检查引用，不能按文件名直接去重。 |
| 实验文档与旧工具 | QuestList 专项文档仍含历次实验说明，退役字节补丁工具也保留参考。后续可进一步分离当前指南与历史档案；不得因整理恢复退役构建路径。 |

不按行数继续切碎已经职责单一的 70～200 行实现。`decompiled/` 和原始素材是参考资产，保留原样；`build/` 中的诊断导出与冻结基线属于已忽略的工作产物，不自动清除。
