<!-- 定位：L2 本轮交付、验收边界与细分提交说明。 -->

# 交付与提交审查

## 用户授权
用户明确要求将当前全部改动写入文档，并拆成大量详细的本地Git提交。范围包括已有暂存的CommonLib子模块配置。无push授权，不改远程。临时备份、日志、个人路径映射及构建缓存不入库；Skill正文和反编译README保持原样。

## 已交付行为
- C++按settings、ui、hooks、hud、questlist拆分；初始化、同步和设置重载保留原有接口。
- 新相对偏移链路，活动实现删除最大高度设置。保留罗盘父子关系、目标状态与完成顺序。
- 原装饰Header和DetailBody统一布局；名称原位渐变、分离名称/距离缩放；文字按固定装饰宽度换行。
- 标定使用独立INI开关与同父级兄弟节点，绕过普通面板的经验遮罩。当前采样支持的候选带为1024根坐标下Y760～960；普通面板上下边界、整行省略和调试统一处理，标定模式不受限制。
- 用户确认候选带部署后不再发生先前的跨区显示。未确认所有视角、横向区域或非1024舞台。
- 最新省略修正：原日志显示目标927/954可放进960底边，但省略号42预留将截止线提前到918。现按item实际比例计算标记尺寸，并保护最后一条可读目标；空间不足时取消标记，而不是取消最后目标。实机验收仍待用户确认。

## 验证记录
最终源码组合的反编译AS2布局/状态模拟56/56、Python24/24通过；三项SWF输入/产物哈希与主包14/CHS1暂存和ZIP检查通过。标定原生接口已完整编译，最新省略修复不改DLL。最新部署只更新两份内嵌Compass，100个受保护文件哈希不变。测试、打包、部署不等于VR实机验收。

## 提交拆分原则
按依赖、构建、INI、原生模块、AS2职责、测试、原始素材、生成工具、配置、发布资源和管理文档分组。每条提交使用Conventional Commits，正文列明路径职责及验证边界。新增入口与模块互相依赖，因此中间提交不保证独立可构建；完整验证针对最终组合。二进制SWF与配套清单作为一个同步发布提交，不将不同生成阶段冒充独立历史修复。

## 保留项
- 不修改用户保存设置、存档、MO2优先级或原始VR网格。
- 标定仍可手动开启；关闭步骤见用户手册。
- 生成目录中的配套SWF是发布输入，需要跟踪；build/dist中的安装ZIP与编译产物不跟踪。
- 后续需实测紧空间目标保留、极长内容省略、镜头运动和不同偏移；必要时进一步标定横向与其他姿态。
