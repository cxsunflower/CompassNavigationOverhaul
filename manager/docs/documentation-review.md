<!-- 定位：L2 当前 project-manager Skill 自检结果与明确标注的历史审查证据。 -->

# 文档符合性审查

## 当前复核：2026-09-13最终整理与分组提交
重新读取指定project-manager Skill、L0/L1和相关L2；保留Skill正文与反编译README。用户重新授权整理文档并多次提交，沿用英文Conventional Commits；此前撤回的4次提交不恢复，按当前完整工作区重新分组。

### 当前实现与验证边界
- 原生：VR根搜索／身份与可见性校验、独立注视门控、英文Hook日志及QL门控原因；武器拔出语义保留。
- UI：退役绝对位置与Y偏移，X为±200；紧凑矢量三点23×5，任意目标子元素越界时整条替换并停止后续；Q/F/V线段显示带保护及每帧重复裁剪优化。
- 默认日志Info（uLogLevel=2），已有保存配置可覆盖默认；本轮不重写用户INI。
- CI：共享手动Build、可选Build+Release、明确run ID发布；版本、产物哈希、来源提交及标签目标校验。两份附件均为ZIP，发布标题和标签规则见开发手册。
- 未解决事项保留：X实机效果、两项标记角度缓存、条件帮助／阈值及中文校验、精确完成时间排序、Palm与VR画面验收；不以测试通过冒充实机完成。

### 当前安装只读观察
本轮提交前只读核对：两份安装Compass、中英文MCM及两份默认INI与当前源输入一致，打包目录的对应文件也一致。此前发现过来源未确认的外部更新；本轮仅冻结当前文件，不把一致性冒充由本轮执行的部署或游戏加载证明。

1842项原生／配置、安装、已有发布输出及只读规则文件按本轮新快照作提交前后比较，不继续套用已经过时的旧部署清单；不删除备份、不回滚外部更新。

### 分组与测试
1. 原生VR节点、门控和诊断。
2. 配套MCM、AS2呈现／省略／性能、SWF与哈希清单，以及字节级输入属性。
3. GitHub Actions共享构建与安全发布、CI打包工具和测试。
4. 功能指南、玩家手册与工具说明。
5. 项目索引、MCM审查、经验与阶段记录。

同一源码文件的累计改动保持成组，不伪造实验历史；SWF与依赖哈希同组，最终组合统一验收。已验证55项Python；两份最终SWF各60项布局、16组观察器、84项距离；actionlint、39项Git字节过滤、真实输入临时打包20／1文件及ZIP校验。37份公开Markdown结构和链接检查通过。GitHub托管runner及实际Release没有执行，本轮不重打包正式输出、不部署、不推送。

[当前索引](../CONTEXT.md) · [当前实现](quest-list.md) · [Actions与发布](../user-manual/development.md#github-actions) · [阶段记录](../stages/s3-documentation.md)

## 历史复核：2026-09-13分组提交之前
按用户指定的project-manager Skill执行：重新读取源规则、L0/L1及相关L2；Skill源和项目副本正文只读，不修改反编译README，不展开隐私映射。

### 当时状态与修正
- 历史已部署：VR根搜索、独立可见性门控、QL原因、摘要／短ID及旧位置退役；当时仅更新DLL和两份Compass，1701项保护文件未变。当前已有后续外部变化，不能继续套用该清单。
- 最新源码：删除QuestList Y偏移、X扩大为-200～200；34个注册键、28项菜单。只读核验安装DLL、两份Compass、中英文MCM及默认INI均匹配最新输入；打包目录关键文件也已更新，更新过程不由本轮执行。
- 未解决：X实机效果、两项标记角度缓存热更新；条件帮助、成对阈值和中文HUD缩写校验仍有已知缺口。Palm隐藏／恢复、空手时列表不出现及标签位置仍待同次启动实机验收。
- 清理当前指南中的旧“VR修订未部署”、旧测试计数和重复部署段；初始MCM审查与阶段历史保留原编号及时间边界。
- 用户明确授权当前累计改动按职责分组使用英文Conventional Commits；不推送、不部署，不把本轮授权外推至未来任务。

### 提交前安装复核
本轮提交前只读检查发现，安装DLL、两份Compass、中英文MCM及两份默认INI与最新工作区产物一致；它们已不同于上次授权部署清单，另有用户保存设置及模组管理元数据变化。文件时间早于本轮操作，变更执行者和备份过程未确认。本轮没有打包部署，不覆盖或回滚这些文件；旧清单仅作历史证据，当前安装以本轮只读快照为准。文件一致不等于游戏已加载或实机验收通过。

旧清单中的3个部署文件和8项保护记录发生变化，另1693项仍匹配。仅记录当前字节哈希和旧清单差异；不猜测更新工具或操作者，不建立虚假的已备份部署记录。

### Skill自检
| 维度 | 结果与边界 |
|---|---|
| 轻量索引 | CONTEXT保留身份、状态和导航，低于80行上限 |
| 当前／历史隔离 | 源码、生成资源、旧打包、实际安装和VR验收分别说明 |
| 阶段记录 | S1／S2历史保留，S3记录本次文档语义整理及分组提交 |
| 可复用经验 | 在既有文档维护主题补充安装基线及测试适用范围，不重复堆叠部署结论 |
| 使用手册 | 玩家入口、MCM、QuestList、HUD与排障指南统一当前状态 |
| 只读边界 | Skill、反编译README、用户INI、Palm及Spell Wheel不在提交改动范围 |
| 验证 | 37份公开Markdown结构及链接检查；语义人工复核，不能替代VR验收 |

### 验证证据与发布边界
最新开发轮已通过49项Python、原生编译以及最终SWF的54项布局／15组观察器／84项距离回归；本轮提交前再次检查最终组合。Python生产方法测试须在VS环境运行，不把无编译器时的skip计为通过。

本轮不重新打包。当前打包目录关键文件匹配最新输入，ZIP尚未在本轮复核；上次20文件／CHS1文件及ZIP校验仅作历史证据。文件哈希与测试通过均不证明游戏内视觉效果；提交结果以git log为准。

### 下一步
先确认当前外部更新来源及有效覆盖，再重启游戏验证新菜单、X端点位移和剩余门控。若需要重新打包部署，仍须单独授权，不回滚用户保存设置。未经授权不修改两项角度缓存或第三方模组。

[当前索引](../CONTEXT.md) · [S3记录](../stages/s3-documentation.md) · [MCM审查](mcm-audit.md)

## 历史审查：2026-09-10
以下保留当时发现、处理和测试数字，仅为历史证据。其中“本轮／当前／新 Debug 尚未部署”均指当时，不能推断现在的源码或安装状态。

### 结论与依据
2026-09-10 按用户指定的 project-manager Skill 复核。**原文档并非完全符合要求**：目录分层已存在，但定位、链接、当前状态和历史隔离存在缺口。本轮已整改发现项，保留 Skill 原文和反编译 README 不变。

### 发现与处理
| 检查项 | 整理前 | 本轮处理 |
|---|---|---|
| L0/L1 低上下文入口 | CONTEXT 未超80行，但堆叠历史部署、测试数字与重复状态 | 收拢为项目身份、当前任务、部署边界和按需导航 |
| 定位首行 | 旧 Passenger 笔记缺失 | 合并进有定位的历史存档，移除旧文件，不留跳转壳 |
| 相对链接 | 旧索引13处路径仍按迁移前位置解析 | 修正为实际相对路径 |
| 工具事实与链接 | 声称存在独立动态面板工具及构建检查，相关链接失效 | 核对文件和 build.bat 后纠正为静态原型边界 |
| 当前／历史分离 | QuestList、MCM、手册混入旧红框、旧缩放范围及“最新已部署” | 当前指南统一新方案；历史材料保存在 guidance-history 并醒目标记 |
| 重复经验 | 观察器原则、TextField提醒重复出现 | 归并主题，交叉引用；新增裁剪、集合与实例状态的反例经验 |
| 操作链路 | 独立普通开关与 MCM 联动说明曾并存 | 现行统一 Debug／Trace 开启，Info及以上关闭；校准独立 |
| 阶段记录 | 修复与文档结果未归档 | S2记录三项缺陷；S3记录审查与待更新清单 |

### 当前状态核对
- 本次线段裁剪、Q可见集合与空列表状态三项修复已从最终嵌入SWF回归验证；普通观察器共12组，布局／校准49项，距离底边84组。
- 新 Debug 尚未部署，之前已部署的距离修复未回退；不把构建、打包、安装与VR验收混为一谈。
- 目标缺少完成时间戳仍是已知需求缺口，本次未顺带修改排序算法。

### 自动检查与复核范围
运行 `python -X utf8 tools/check_manager_docs.py`：检查 manager 定位首行、标题、L1行数、本项目版本残留、第一方 Markdown 本地链接和标题锚点。配套 `tools/tests/test_manager_docs.py` 检查正反例；开发手册已纳入命令。

本轮还人工核对当前 Debug／MCM／QuestList 指南与源码、菜单帮助、构建脚本和部署记录。Skill与privacy仅在结构扫描中读取首行元数据，不加载隐私映射；不为凑模板生成空的Web API／数据库文档。

### 最终验证结果
- 36份第一方公开 Markdown：结构、文件链接及标题锚点检查0错误；CONTEXT为35行。
- Python共30项通过，包含6项文档检查回归；SWF观察器12组、布局／校准49项、距离84组通过。
- 主包18文件／CHS1文件及ZIP校验通过；114个安装／配置文件未变，Skill副本、反编译README及正常布局／距离源码保护校验通过，Git索引为空。

### 保留与限制
- Skill 原文及项目副本正文只读；根 AGENTS 既有规则不改；反编译 README 不改。
- 历史存档的旧结论保留为证据，不宣称逐条重新验证；存档内“当前／最新”必须按当时记录解释。
- 自动检查不是完整语义证明，也不验证外部网站可用性或VR运行结果。后续每轮变更仍需人工核对当前状态与指南。
- 本轮不部署、不暂存、提交或推送，不改用户配置与第三方 Palm。

[当前索引](../CONTEXT.md) · [S3记录](../stages/s3-documentation.md) · [整理前材料](guidance-history.md)
