<!-- 定位：L2 用户要求按 project-manager 重写文档的结构化记录。 -->

# S3：项目文档分层（已完成）

## 目标
按用户提供的 project-manager Skill 重写文档，支持低上下文接手和玩家/开发者分角色上手；不改程序、不提交 Git。

## 设计
- 保留根 AGENTS 的既有内容，追加项目管理路由；L0+L1 控制在 100 行以内。
- CONTEXT 仅写身份、状态、阶段及导航；详细行为与日志进入 L2。
- 迁移 QuestList、MCM 与历史全文至 manager/docs；按用户后续要求移除根 docs 跳转页，所有入口直接指向 manager。
- 手册区分玩家操作、环境搭建、开发和排障；不套用无关 Web API、数据库或生产服务器模板。
- 原始 Skill 正文保留，初始化添加定位首行；之后按只读副本维护。
- 不收集新的私密信息，privacy.md 仅留空模板并忽略；不记录 Bridge 地址。

## 实现与复盘
- [x] L0/L1 路由、项目约定、阶段记录及经验文档。
- [x] 专项文档与使用手册；根目录旧 docs 合并移除，引用统一。
- [x] 纠正“尚未部署”的过时绝对表述，保留实际安装检查与完整部署验收的区别。
- [x] 当时按证据把 MCM Helper 故障标为未解决；后续 S2 已取得成功注册日志，当前文档已改按最新状态表述。

## 待更新文档
无本轮未同步项；后续开发按当前 S2 更新，不为小修补重复新建阶段。

## 后续整理：旧 docs 完全合并
用户明确要求旧 docs 合并至 manager。确认旧目录仅余四个跳转/索引页，正文及历史已存在于 manager/docs 后删除这些旧页，并更新 README、AGENTS 与 CONTEXT；保留技术正文和历史档案，不更改 Skill 副本。

## 文档去版本号
按用户要求移除本项目的具体版本号，当前状态、历史说明与参考基线使用角色名称；版本信息仍以 vcpkg.json 为准。保留依赖工具版本、日期及运行时诊断证据，不修改程序版本配置。

## 2026-09-10 Skill 符合性复核与整理
用户要求修复 Debug 后按指定 project-manager Skill 复核文档。查到旧索引13处失效相对链接、工具说明1处不存在的独立面板链接／构建指令、一份缺定位首行的旧 Passenger 笔记，以及当前指南混杂旧缩放／红框／部署结论。
收拢 CONTEXT 为身份／状态／导航；QuestList、MCM 和玩家入口统一现行规则，旧指南与孤立笔记合并进 guidance-history，删除旧笔记文件而非保留跳转壳；补正历史索引链接、工具模块数量和观察器职责。经验按主题合并并记录三项反例，不把新摘要当作旧正文的修复。
新增 `tools/check_manager_docs.py` 与回归，检查定位、L1、版本残留、第一方文档链接及锚点。Skill正文、反编译README、用户隐私与安装配置未修改；结构通过不表示历史每个实验结论或VR效果都已重新证实。
待更新文档：本次索引／阶段／专项／手册／经验／工具导航已同步；具体符合性与例外见 [审查报告](../docs/documentation-review.md)。

## 2026-09-10 英文分组提交授权
用户明确授权将当前累积改动分多次 Git 提交，且标题与正文使用英文。按独立原型、原型文档、距离修复、HUD诊断实现与产物、文档检查、历史归档、当前指南、项目记录八组组织；不伪造此前实验的提交历史，中间组可能依赖后续资源／文档，完整验证针对最终组合。之前各节“未提交”描述保留为当时状态；此次授权不包括推送或部署。提交前复查30项Python、文档结构、三项SWF哈希及主包／CHS与ZIP一致性。
## 2026-09-12：按指定 Skill 统一当前文档
用户明确要求使用 project-manager Skill。重新读取指定源文件、L0/L1 与相关 L2；继续在 S1/S2/S3 内记录，不为小修补新建阶段。直接修正当前指南中“已部署／尚未部署”混用、旧 Visible 语义、Palm 已修复暗示及短 ID 边界遗漏，不只在文末追加相反摘要；历史阶段保留当时事实并链接当前指引。

- [x] CONTEXT 仅保留身份、状态和导航，低于 80 行；区分当前安装与最新构建。
- [x] S1/S2 关键决策和待验收项同步，S3 记录文档更新范围。
- [x] HUD／QuestList 专项、排障和玩家手册同步英文诊断、日志入口与适用版本角色（不写项目版本号）。
- [x] experience 按主题沉淀节点身份／门控证据及摘要／短 ID 边界一致性，更新遗留问题，不重复记录偶发工具故障为经验。
- [x] Skill 源及副本、反编译 README、隐私映射、源码与安装文件不在本轮修改范围；不部署、不暂存、不提交或推送。

待更新文档：本轮相关指南已同步；后续根据部署授权与 VR 验收事实更新，不把模拟通过标记为实机通过。自检使用 tools/check_manager_docs.py，并检查链接、定位首行、索引行数、状态措辞与 git diff --check。 同轮复核补齐此前遗漏的 documentation-review 当前结论：旧审查数字明确归为历史；开发手册补入生产方法编译测试、跳过判定和临时依赖绕过边界。未重复创建经验或阶段条目。

## 2026-09-13：Skill复核与分组英文提交
用户授权“用skill项目文档管理，然后分别多次进行git commit，gitcommit英文”。重新读取指定Skill与项目L0/L1，按需复核MCM审查、功能指南、玩家手册及阶段记录，不修改Skill正文。

清理当前文档中的过期未部署判断、重复部署段与旧测试统计；明确旧绝对位置删除及VR诊断已部署，后续Y删除／X扩大至±200已构建但未打包部署。保留X实机待查、角度缓存、条件帮助／阈值、中文校验及排序需求缺口，不把静态通过改写成实机完成。

本批分为四个职责组，英文Conventional Commits：原生VR节点与门控；配套MCM／AS2布局诊断及生成资源；功能与玩家指南；审查、索引、经验及阶段记录。AS2源码、配套SWF和哈希清单同组，跨组回归结果以最终组合为准；具体哈希以git log为准，不伪造先前实验的独立提交历史。

验证沿用最新开发轮49项Python、原生编译、54项最终SWF布局、15组观察器、84项距离回归，并在提交前复核；文档结构检查37份、0错误。上次包不代表最新源码，本次不重打包、不部署、不推送。用户INI与第三方模组不改。

### 待更新文档
- [x] CONTEXT与审查报告明确源码／打包／部署边界及本轮授权。
- [x] MCM、QuestList、HUD、玩家入口与排障状态同步。
- [x] 经验按主题归并，原始审查及历史阶段保留时间边界。
- [x] 生成资源、测试与文档进入对应提交组；结果由Git日志核查。
### 提交前补充：安装清单失效
本轮提交前只读检查发现，安装DLL、两份Compass、中英文MCM及两份默认INI与最新工作区产物一致；它们已不同于上次授权部署清单，另有用户保存设置及模组管理元数据变化。文件时间早于本轮操作，变更执行者和备份过程未确认。本轮没有打包部署，不覆盖或回滚这些文件；旧清单仅作历史证据，当前安装以本轮只读快照为准。文件一致不等于游戏已加载或实机验收通过。
旧清单比较：3个部署文件及8项保护记录不同，另外1693项仍匹配。当前安装和打包目录关键输入已与最新源码／构建一致；不沿用前文“尚未打包部署”推断现在的文件状态。新增只读快照用于本轮提交前后保护核对，不冒充授权部署或回滚备份。

## 2026-09-13：撤回提交后的代码与文档清理
按用户“清理代码，整理文档”授权，仅整理本轮AS2增量：分离一次性三点绘制、删除目标逻辑边界重复转换、将同轮视口查询移出目标循环、修正过时注释和测试名称。不删除有效回退或诊断，不修改原生源码、用户配置及Palm。

当前QuestList指南移除重复的历史修复正文，使用S1中的原记录；CONTEXT、审查报告、MCM与玩家入口同步未部署新改动和4次提交已撤回状态。保留Skill正文、反编译README、旧备份及发布包。更新后的源码重新生成配套SWF并跑相同回归，不将清理当成功能变更。

验证范围：49项Python、59项布局、16组观察器、84项距离、37份文档及1842项保护文件快照。未暂存、提交、打包部署或推送。

### 本轮待更新文档
- [x] 当前源码职责、三点尺寸及坐标约定。
- [x] 当前／历史验证和部署证据分离。
- [x] 撤回提交后的状态与只读边界。

## 2026-09-13：GitHub Actions对齐当前工程
用户授权更新GitHub Actions。移除已删除VERSION及旧汉化路径依赖，Build变为手动／可复用共享流程，Build+Release复用Build与Release，独立Release要求明确成功运行ID。加入递归子模块、工具链、文档／源码／SWF检查、固定校验和JPEXS对两份最终电影的回归；打包与ZIP校验统一到ci.py。发布校验手动运行来源、来源SHA、清单版本、两份ZIP哈希及已有标签目标，不从文件名猜版本，也不默默选最近构建。

补齐两个字节级输入的Git属性，确认39个清单输入经过Git过滤后字节保持。新增6项CI测试，全量55项Python通过；actionlint通过；两份SWF各60项布局／16组观察器／84项距离通过；真实20／1文件包在临时目录核验。未替换正式包、未部署、未暂存提交或推送，未触发GitHub运行与Release。GitHub托管环境仍需后续授权上传后验证。

## 2026-09-13：重新授权Skill整理及五组英文提交
用户要求“skill整理文档，分段多次提交”。重新读取规则与当前工作区，保留此前撤回状态，不恢复旧四次提交；按当前职责重新拆分为原生、UI及配套资源、CI、功能指南、项目记录五组。英文Conventional Commits，源码／SWF／哈希同组；不推送、不执行部署或Release。

本轮提交前只读核对：两份安装Compass、中英文MCM及两份默认INI与当前源输入一致，打包目录的对应文件也一致。此前发现过来源未确认的外部更新；本轮仅冻结当前文件，不把一致性冒充由本轮执行的部署或游戏加载证明。

当前指南修正过期“仅在源码／未部署”推断；补齐两份ZIP、Release标题及Git标签格式。当前审查统一55项Python、双SWF各60／16／84回归、actionlint、39项字节过滤与临时20／1文件打包验证，保留全部实机缺口。1842项新快照用于本批提交前后保护核对。提交结果以git log为准。

### 本批待更新文档
- [x] 当前索引、安装观察与最新验证边界同步。
- [x] 三点、完整目标、热路径和Actions指南对齐实现。
- [x] 发布附件／标题／标签格式补齐。
- [x] 旧提交撤回与本批重新授权分别记录，历史阶段不改写为实机通过。

## 2026-09-13：CI全新检出发现Journal XML换行漏提交
用户提供Build中的QuestList SWF/source mismatch。逐项比较三个清单、工作区和HEAD blob：唯一不匹配为journal-shapes.xml；工作区与清单使用295个CRLF，HEAD仍为LF，归一化后内容相同。上一轮只验证过滤结果，遗漏了新增属性不会自动更新已跟踪blob这一点。

保留现有XML原始字节、SWF和清单，重新写入相同字节使Git识别待提交的换行差异；用隔离索引导出修复候选树，验证全新目录中的三份SWF检查及Python测试。此次修正不自动提交、不推送、不部署；旧HEAD及已失败run不会因此自行修复，需要提交推送后重新运行Build。
