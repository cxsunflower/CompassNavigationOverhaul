<!-- 定位：L2 对既有 QuestList 工作的追溯整理，不虚构新增开发阶段。 -->

# S1：QuestList 排序与装饰

## 目标与边界
用户要求已完成目标反序、目标分隔装饰黑边增强；随后明确菱形不加粗。不恢复已撤回 Palm UV/网格补偿。

## 设计与决策
- 进行中优先，完成组按原始目标序号倒序，失败组原序。当前数据无完成时间戳，未承诺精确恢复非线性历史。
- 分隔装饰黑色 Glow 调为 8/8/8；菱形曾误加强，依用户纠正恢复 4/4/4，尺寸 18×27 不变。
- 保持 Normal 字体，避免中文字体包缺少粗体字形的问题。

## 实现与验收
- [x] 源码修正、SWF 重编译、嵌入两份 Compass。
- [x] 11/11 反编译布局/状态测试通过；哈希校验及打包完成。
- [ ] VR 字形、描边、位置、排序、门控与缩放实机验收。

## 复盘
需求中的“装饰”应区分分隔线和状态图标，避免扩大修改范围。源码测试之外必须验证实际编译资源；见 [经验](../docs/experience.md)。完整历史不删改，保存在 [档案](../docs/quest-list-history.md)。

## 待更新文档
当前实现已同步；后续实机结果到来后再更新 [QuestList](../docs/quest-list.md) 的验收状态。

## 后续用户修正
分隔装饰改为参考任务详情标题装饰：直接复用 4/4/4 黑色滤镜，去掉额外灰色扩边；不修改菱形及文字。重新构建、嵌入、11 项布局测试和打包完成，实机验收仍待进行。

## 2026-09-10：原生日志矢量独立皮肤
用户选择不运行时引用日志装饰、不制作 Journal 桥接补丁；授权原始素材提取＋独立皮肤，并要求预览图。目标图标和目标分隔装饰替换为 Journal 原始矢量，旧描摹 SVG 不再参与构建，上方任务详情装饰保持原字节。

- 按 `ObjectiveListItem` 状态及 `objectivesHeader` 名称定位，只保留四个纯 Shape；提取源记录 SHA256，不修改安装的日志文件。
- 两份 `!assets/QuestItemListSkin.swf` 提供固定导出接口；图标等比纳入 18×27 槽位，失败状态使用日志原始叉号。不加载日志页面或脚本，不修改 DLL、布局算法或 Palm。
- 已通过 38 项 Python、50 项最终反编译布局／状态模拟、12 组观察器及 84 项距离底边回归；原始图形路径／颜色经 SWF 往返检查。
- 预览使用最终皮肤 SWF 导出的实际矢量合成，并明确标注非游戏截图；不作为 VR、字体、滤镜或 MO2 加载验证。
- 预览完成时尚未授权部署或提交；新皮肤及之前的新 Debug 继续未安装。普通日志美化包不会自动同步，需提供[独立皮肤适配](../../assets/source/questlist/art/README.md)。

## 2026-09-10：清理与提交授权
用户随后授权“清理整理下，提交 git”。清理范围限定本轮原生皮肤：移除已退出构建的三份描摹 SVG，历史保留在 Git；补齐资源目录、两套素材来源与运行时导入方式、打包要求及当前状态说明。上方详情素材最终作者来源未证实，不把“项目原件”等同于“游戏原版”。保留无关缓存、旧 Debug 备份、用户配置和第三方模组。

按职责分组使用英文 Conventional Commits 本地提交，源码、生成 SWF 与哈希清单保持成套；不推送、不部署。提交前复核构建清单、Python 与最终反编译回归、文档、打包和安装保护快照。提交结果以 git log 为准。

## 2026-09-12：文字阴影与罗盘样式统一
用户明确只关注阴影、模糊和黑边，并确认采用罗盘名称／距离同款参数。只替换详情正文、目标行文字和“目标”标签的滤镜：DropShadow 距离 2、角度 45°、黑色、不透明度 1、模糊 2×2、强度 1.5、质量 2，关闭内阴影、挖空和隐藏文字；不叠加旧 Glow。

字体、字号、字距、文字颜色、布局和跟随不变，图标 4/4/4 Glow、原装饰及省略标记不变。回归新增滤镜类型／完整参数、仅一层效果、中英文标签、不同状态与重排后的保持检查；最终反编译布局／状态 51 项通过。既有原生皮肤预览不是本次阴影的实机证据。构建与模拟不等于 VR 复杂背景可读性验收；本轮未授权部署或提交。

## 2026-09-12：省略标记与目标文字列对齐
用户确认“. . .”前面应留出目标图标及间距的空白，与目标文字而不是菱形左边缘对齐。采用逻辑槽位，不绘制假图标或插入受字体影响的空格。测量最后可见任务中真实目标文字字段的原点，经全局坐标转入布局空间；无可见目标行时使用正文容器内 x=26 的目标列。保留现有比例及不挤掉最后可读目标的规则，横向不足时不显示标记。

新增 90%／110% 缩放与目标文字列变更后的对齐测试，最终反编译布局／状态 52 项通过。延续前一轮文字阴影改动，不修改省略标记本身的字体或滤镜；未部署、未提交。

## 2026-09-12：截图反馈与真实 TextField API 修复
用户指出 QuestList 截断后没有省略提示。核对时安装目录的 Compass SWF 与修改前项目产物一致，不能简单归因于未安装旧修改；未据此推断截图时进程加载状态。

省略对齐曾错误调用 TextField.localToGlobal，而 AS2 文本字段不具备该接口。模拟器继承 MovieClip 接口掩盖问题；新增禁用字段坐标方法的反例，在原最终 SWF 上复现 `localToGlobal is not a function`，52/53 通过。改为父 MovieClip 转换字段的 x/y 后，53/53 通过，并修正测试本身的相同错误。逻辑空图标位及不挤掉最后可读目标规则保留。

这修复明确的省略定位中断，不声称单凭截图已定位全部物理视口／UV 裁切；只有逻辑范围外裁切或无安全空间时，仍须日志与实机核查。本轮未部署或提交。

## 2026-09-12：部署状态更新
用户授权后，文字阴影、英文诊断及省略标记对齐／真实 TextField API 修复已随两份内嵌 Compass 和配套 DLL 部署；旧记录中的“未部署”描述对应当时状态。独立皮肤与上方详情资源保持现有一致内容。[备份、文件范围与校验记录](s2-mcm.md#2026-09-12授权部署本轮累积修复)。VR 实机验收未完成，本轮改动未提交。
## 2026-09-12：Palm 隐藏门控与 Debug 描述右移
用户反馈 Palm Compass 已从手中隐藏，低头仍显示任务列表；同时要求两行 Debug 文字稍往右。检查保存配置确认注视与可见性要求均已开启，不修改用户 INI 或第三方 Palm／Spell Wheel。

根因是 Flash 可见状态的正向提前返回绕过 3D 节点／祖先 AppCulled 检查；另修复关闭注视要求同时跳过可见性要求的独立漏洞。已知隐藏优先否决；节点缺失保留旧 Flash 回退，不改变注视模式的既有回退策略。Debug 左候选 x=12→60，仅摘要文字，不移动诊断区域和游戏内容。

提取生产方法体并用 MSVC 测试替身编译：旧代码明确失败，新代码 15 个判断通过；VS 环境 45 项 Python 通过。最终内嵌 SWF 反编译后 53 项布局、13 组观察器、84 项距离回归通过；完整 DLL 编译与主包／CHS／ZIP 校验通过。此次新增 DLL／SWF 尚未部署，无暂存、提交或推送。前次五文件部署仍为安装状态；需后续授权部署，再验证 Palm 隐藏／显示、注视关闭但可见性开启、节点恢复及文字是否仍被实际 VR 视口裁切。
## 2026-09-12：授权部署 Palm 门控与 Debug 右移
用户随后授权“部署下”。确认游戏进程未运行、资源清单和归档通过后，仅更新现有主模组 DLL、HUDMenu／VR_HMD_info 两份 Compass，共 3 文件；未创建新模组目录。备份 `../_patch_work/compass-palm-debug-deploy-20260912-215616` 包含旧文件与 manifest.json；3 个新文件／旧备份哈希验证通过，1695 项保护文件保持不变（含设置、汉化、第三方 Palm 与 Spell Wheel）。当前安装已包含这轮修复，前节“未部署”是授权前记录；VR 实测仍待完成，无暂存、提交或推送。
## 2026-09-12：实机未通过后的 VR 节点查找与可解释门控
用户明确：罗盘还在但 QuestList 不出现；另外 Palm 隐藏时低头仍出现列表。上次部署不是成功验收。通过 Windows MyDocuments 特殊目录定位真实 SKSE 日志：Palm 选中 PlayerWorldNode/…/skyVR_HMD_Compass.nif/skyVR_HUD01；CNO 一直报 camera/player roots 找不到节点，42 次注视采样全部 mode=2/0。说明之前的 AppCulled 否决没有命中实际节点，Flash 回退和俯角模式仍在工作。

新增 VR 场景根搜索、compass NIF 祖先路径核验、重复根去重、唯一候选要求、缓存归属检查与独立实时时钟重试。VR 缺失节点不再由 Flash 判为可见；非 VR Flash 回退保留。右手回退优先 NPCRHnd，3D 距离起点使用 HmdNode。武器拔出行为与保存设置不改；空手却不显示的具体门控尚不能由旧日志证明。

诊断拆分：Info shown 仅指名称／距离动画，QL 原生门控原因另列；requested 不承诺实际渲染。详细 [QuestListGate] 记录原始武器状态、配置和全部门控，避免短路造成注视状态陈旧。两行摘要改为 M 边框右侧优先的固定安全槽位，不再使用 Stage 边缘，碰撞时隐藏，不追逐距离动画。新代码尚未部署或提交，需构建／回归完成后另行部署验收。

构建环境出现与源码无关的变化：vcpkg 要求下载新的 CMake，强制系统工具又遇到 WindowsApps/pwsh.exe 不可访问；复用现有已安装依赖，临时关闭构建目录的 manifest install 进行编译。旧 PCH 报编译器版本不匹配，因此正在清理重编本项目和 CommonLib；未改系统工具、第三方模组或项目依赖清单。
验证完成：47 项 Python（包含实际可见性方法 16 个判断、VR 搜索／缓存方法 8 个判断），最终内嵌 SWF 反编译后 14 组观察器、53 项布局、84 项距离回归通过；本项目与 CommonLib 清理重编成功，主包 20 文件／CHS 1 文件及 ZIP 一致。临时 manifest install 绕过已恢复为 ON；外部工具下载／pwsh 问题并未由本次任务修复。证据与构建日志保存在忽略的 build/journal-art-review/vr-gate-*。最新修复未部署、未暂存／提交／推送；空手时隐藏的具体门控仍需新日志实测。
## 2026-09-12：短 ID 标签显示带漏检
用户反馈 D/H 跑到游戏画面最上方。源码确认：摘要受连续显示带约束，短 ID 只检查 Stage 边界和碰撞，且优先选对象上方，存在显示带漏检；尚无对应实机坐标证据，不把此漏洞当作已证明的全部 UV 根因。

修复覆盖 D/H/M/N/Q/F/V：候选顺序右、下、左、上，所有候选必须完整落在既有显示带内并避让；没有安全候选或显示带未知时隐藏，日志记录 no-safe-label-slot，不把标签夹到远离自身边框的位置。不移动对象、边框或任务列表，不修改英文 Debug 文案。最终内嵌 SWF 反编译 15 组观察器、53 项布局、84 项距离回归通过，47 项 Python 通过；沿用上轮已完整重编的 DLL，重新打包和 ZIP 校验通过。本增量和上轮 VR 根节点修复均尚未部署／提交，实际显示仍待 VR 验收。
## 2026-09-12：编译打包并授权部署当前产物
2026-09-12 本次授权部署已完成：现有主模组 DLL／HUDMenu 与 VR_HMD_info 两份 Compass 共3文件已备份更新，1701项保护文件保持不变。INI、CHS、Palm和Spell Wheel未改；新DLL不再读取旧位置键，现有INI可仍保留这些无效条目。偏移Y抵消、X实机效果及两项标记角度热更新缺陷仍未修复，不将部署视为实机验收。

原生构建确认无待编译文件，48项Python通过；重新从发布包导出实际SWF，15组观察器、53项布局、84项距离回归通过，主包20／CHS1与ZIP一致。中文校验器HUD缩写问题仍是已知独立问题，未声称其已修复。临时依赖安装绕过已恢复ON。

备份在项目同级 `_patch_work/compass-current-deploy-20260912-235729`，含旧文件与manifest.json；执行记录 build/journal-art-review/current-deployment-result.json。以部署前即时哈希备份实际安装内容，不使用旧清单覆盖后来变化。3个新文件和3个旧备份哈希均验证，其他文件未改。未暂存、提交或推送。

## 2026-09-13：省略空隙与Q/F/V跨带线段
2026-09-13本轮修正（未打包部署、未提交）：省略标记先尝试最后可见内容下方的实际空隙，按实际文字边界校验，成功时不缩减内容上限；空隙不足才回退原预留方案，保留完整正文及最后可读目标优先。Q/F/V绘制线段额外限制在列表既有显示带内，描边内缩1个逻辑单位，未知／无效显示带不画列表框；不补画裁剪边界，不改变日志完整几何或D/H/M/N的Stage保护。未扩大安全区、未改Palm。旧SWF新增反例失败，新最终SWF通过55项布局、16组观察器、84项距离回归；49项Python及原生构建检查通过。实机仍待验收。

调查证据：日志记录omission=preserve last readable objective、visibleRows=1、totalRows=3；完整F底边约986.4，而列表候选带止于960。截图旧Visible文案与当前Info shown／QL文案不同，不能断言截图、当前日志、源码来自同一加载状态。此次修复针对已确认的算法和裁剪缺口，不宣称截图中的那条线已完成实机定位。

## 2026-09-13：任意目标整条替换与三点实际高度
2026-09-13紧凑省略标记修订（未打包部署、未提交）：省略标记改为三个纯矢量点，不再用整行TextField测量。每点3×3白芯加1单位黑边，整体可见边界23×5；不加Glow／阴影滤镜，没有字体行高或额外上下间距。随条目缩放后，高度为5×0.65×实际面板比例，90%／100%／110%时分别2.925／3.25／3.575个SWF逻辑单位，不等于VR屏幕像素。空图标槽仅保留横向对齐，无独立高度。

用户澄清object1/2/3只是举例，规则适用于任意目标。现在合并逻辑行、实际文字和图标边界检测（同一坐标系，含保守余量），任意部分越界即隐藏整条和后续目标；跨任务条目也在首次省略处停止。标记位于首个隐藏目标的起始位置。空槽不绘制，也不保留菱形高度。完整内容恢复后自动清除省略状态。

旧SWF对四组新增反例失败；最新最终嵌入SWF通过59项布局、16组观察器和84项距离回归，49项Python通过。覆盖90%／110%及其他尺寸下的实际点边界、首／中／尾目标、文字和图标0.01逻辑单位的横向／底部越界、后续任务抑制和恢复。回归模拟不证明游戏字体像素或VRUV效果；采用显式矢量避免无法精确取得字体点号墨迹边界的问题，三点外观须实机确认。未打包部署、未暂存提交，HEAD仍01fb780。

## 2026-09-13：省略与裁剪热路径优化
用户授权“优化下代码”。在不修改门控、默认Info、目标完整性、省略大小或位置规则的前提下，删除Update中重复的ApplyHeightLimit：PrepareOverflowLabel已完成最终裁剪或恢复。将回退快照和标记高度测量移到真实回退分支；可见目标计数改为存在性短路。正常帧及直接三点替换不分配两个回退数组。

新增调用计数与数组分配回归；同一最终SWF完整回归为60项布局、16组观察器和84项距离；Python49项。源／生成／嵌入清单配套重建。不把调用减少推断为已测得帧率提升；不部署、不暂存提交，不修改用户INI、Palm或原生逻辑。
