<!-- 定位：L2 对既有 QuestList 工作的追溯整理，不虚构新增开发阶段。 -->

# S1：QuestList 排序与装饰

## 目标与边界
用户要求已完成目标反序、目标分隔装饰黑边增强；随后明确菱形不加粗。不恢复已撤回 Palm UV/网格补偿。

## 设计与决策
- 进行中优先，完成组按原始目标序号倒序，失败组原序。当前数据无完成时间戳，未承诺精确恢复非线性历史。
- 分隔装饰黑色 Glow 调为 8/8/8；菱形曾误加强，依用户纠正恢复 4/4/4，尺寸 18×27 不变。
- 保持 Normal 字体，避免中文字体包缺少粗体字形的问题。

## 实现与验收
- [x] 源码修正、SWF 重编译、嵌入两份 Compass。
- [x] 11/11 反编译布局/状态测试通过；哈希校验及打包完成。
- [ ] VR 字形、描边、位置、排序、门控与缩放实机验收。

## 复盘
需求中的“装饰”应区分分隔线和状态图标，避免扩大修改范围。源码测试之外必须验证实际编译资源；见 [经验](../docs/experience.md)。完整历史不删改，保存在 [档案](../docs/quest-list-history.md)。

## 待更新文档
当前实现已同步；后续实机结果到来后再更新 [QuestList](../docs/quest-list.md) 的验收状态。

## 后续用户修正
分隔装饰改为参考任务详情标题装饰：直接复用 4/4/4 黑色滤镜，去掉额外灰色扩边；不修改菱形及文字。重新构建、嵌入、11 项布局测试和打包完成，实机验收仍待进行。

## 2026-09-10：原生日志矢量独立皮肤
用户选择不运行时引用日志装饰、不制作 Journal 桥接补丁；授权原始素材提取＋独立皮肤，并要求预览图。目标图标和目标分隔装饰替换为 Journal 原始矢量，旧描摹 SVG 不再参与构建，上方任务详情装饰保持原字节。

- 按 `ObjectiveListItem` 状态及 `objectivesHeader` 名称定位，只保留四个纯 Shape；提取源记录 SHA256，不修改安装的日志文件。
- 两份 `!assets/QuestItemListSkin.swf` 提供固定导出接口；图标等比纳入 18×27 槽位，失败状态使用日志原始叉号。不加载日志页面或脚本，不修改 DLL、布局算法或 Palm。
- 已通过 38 项 Python、50 项最终反编译布局／状态模拟、12 组观察器及 84 项距离底边回归；原始图形路径／颜色经 SWF 往返检查。
- 预览使用最终皮肤 SWF 导出的实际矢量合成，并明确标注非游戏截图；不作为 VR、字体、滤镜或 MO2 加载验证。
- 预览完成时尚未授权部署或提交；新皮肤及之前的新 Debug 继续未安装。普通日志美化包不会自动同步，需提供[独立皮肤适配](../../assets/source/questlist/art/README.md)。

## 2026-09-10：清理与提交授权
用户随后授权“清理整理下，提交 git”。清理范围限定本轮原生皮肤：移除已退出构建的三份描摹 SVG，历史保留在 Git；补齐资源目录、两套素材来源与运行时导入方式、打包要求及当前状态说明。上方详情素材最终作者来源未证实，不把“项目原件”等同于“游戏原版”。保留无关缓存、旧 Debug 备份、用户配置和第三方模组。

按职责分组使用英文 Conventional Commits 本地提交，源码、生成 SWF 与哈希清单保持成套；不推送、不部署。提交前复核构建清单、Python 与最终反编译回归、文档、打包和安装保护快照。提交结果以 git log 为准。
