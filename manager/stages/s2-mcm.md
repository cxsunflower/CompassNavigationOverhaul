<!-- 定位：L2 当前 MCM 阶段，记录配置修复和未解决的菜单故障。 -->

# S2：MCM 配置与菜单恢复（进行中）

## 2026-09-10 省略号挤掉全部可读目标
02:51日志中前两条文字底边927.42/953.74均低于960，但34高省略字段加8间距预留把内容底边降到918，导致全目标隐藏。省略字段现跟随实际item比例SCALE*effectiveScale/100（90时58.5%）；宽高按真实显示尺寸测量。额外保护每项任务最后一条原本可显示的目标，若省略号会将其挤掉，则恢复未预留时的完整行并不画省略号。保护正文规则保留；不改位置、候选带760～960、面板缩放或目标行排序。
模拟器补正TextField显示宽高包含自身缩放、textHeight保持本地测量，避免假测试掩盖省略字段尺寸。56/56反编译模拟、24/24 Python、SWF哈希和打包通过，两份Compass部署哈希一致；100个受保护文件未变，备份`_patch_work/compass-debug-scale-20260910-025957`。DLL未改，未暂存/提交，实机待重启验证。


## 2026-09-10 根据采样限制连续显示带
实机标定记录：中心X=512的Y740在罗盘附近、750已在上方，745标记不完整不能硬判；X400/600的Y740仍在下方，760～900位于上方。Y900起步长20采样，图中可辨900～980，1000/1020未见。说明共享显示路径存在分段；这些是采样点观察，不是全宽、所有姿态已标定的UV矩形。

普通Passenger采用候选根坐标带Y760～960（1024舞台），不再只减去底部8%。上下界约束布局、完整段/行省略、面板遮罩与debug；窗口只能在带内移动，不能为保留220高度跨到上方接缝另一侧。固定宽度、原始装饰、110期望上限/min(90,期望)下限不变。父级仍为Target，偏移在安全窗口内受限；窗口已占满可用高度时，纵向偏移可能被边界钳制。

对转换后的局部窗口，在左右两个X端点分别求满足根坐标四边的Y区间，避免旋转情况下直接用逆变换AABB误放行。标定独立节点与dedicated分支不使用此限制；GetSharedHudSafeTop/Bottom按舞台高度归一化换算，非1024尺寸仅为比例假设，未实机验证。横向仍使用已有逻辑宽度/舞台保护，不宣称全宽已标定。

新增变换父级旋转±12度、缩放和±100偏移测试；54/54反编译模拟、24/24 Python、完整构建（DLL已最新）/双语/打包校验通过。仅两份嵌入Compass部署哈希一致，100个受保护文件不变，备份`_patch_work/compass-debug-scale-20260910-024721`。未暂存/提交。

验收：用户将bCalibrateQuestList改0并重启，比较上方完整装饰、正文、目标、省略号与跨区残片。候选带仍待实机验证，不保证任意长度任务全部显示。


## 2026-09-10 独立边界标定模式
新增INI独立开关 `bCalibrateQuestList:Debug`，默认false；axis/start/step/cross默认0/700/40/512。原生注册、分发、启动/重载同步至SetCalibration，记录QuestListCalibration状态。不开新MCM控件，不写回保存设置。

开启时正常QuestList透明度为0、普通调试框隐藏并暂停布局求解；标定节点是同Target父级下的独立兄弟节点，用根坐标短十字/标签采样，不经过QuestList遮罩或8%保护。保留祖先可见性与实际显示路径，关闭/卸载移除标记并恢复正常布局。此为人工采样工具，不代表已测得边界，不修改UV网格。

同时包含上一轮尚未部署的固定锚点修正：不再为保留220逻辑高度而整体上移，而是在原锚点以下与安全底边相交取剩余高度；顶部装饰消失的实际原因及恢复效果仍需实机确认。

53/53反编译布局/状态模拟、24/24 Python、完整DLL重编、双语和主包14/CHS1打包校验通过。14文件已部署且哈希一致，88个受保护文件不变；备份 `_patch_work/compass-full-deploy-20260910-021019`。标定保持默认关闭；未暂存/提交。首次使用须重启。详见[标定操作](../user-manual/calibration.md)。


## 2026-09-10 目标底部跨到上方：恢复共享HUD保护
用户确认分离红紫线对应下方目标区域，不能作为独立调试图形处理。01:43实机快照：期望100/实际90，完整底边1018.31、mask底边1012，首目标文字底边959.32；后两行已隐藏、省略号可见。该日志证实仍放行到原有底部保护之外，但不构成精确UV边界标定。

Passenger窗口与FitsViewport重新使用 `Stage.height-max(20,Stage.height*0.08)` 的共享HUD底边，1024时为942.08。窗口必要时整体上移，保持现有逻辑宽高与罗盘层级；mask和debug线段同步限制，专用独立影片不应用共享HUD保护。维持自动下限90和主动低于90的设置，仍按完整段/行省略。锚点增加0.1界面坐标余量，避免Scaleform twip舍入把4px边距误判溢出。

日志将原本错误标注Root的实际布局空间值更名为safeLimitLocal/contentLimitLocal/visibleBottomLocal，并明确layoutSpace及sharedHudSafeBottomRoot。新增按实机父级Y=706.45的回归，检查90下限、目标可见行、mask与debug底边及偏移后保护。

52/52真实反编译模拟、24/24 Python及完整build.bat/打包校验通过（DLL已最新）。两份内嵌Compass增量部署哈希一致，100个受保护文件不变；备份`_patch_work/compass-debug-scale-20260910-014624`。未暂存/提交。需重启实机对比同任务的目标、省略号和上方残片；不能宣称VR映射已修复或所有内容保证完整显示。


## 2026-09-10 调试白块与 Passenger 过度缩小
- 实机日志：期望 panel=110，实际 effective=25。Passenger 配置单独使用 scale=70/minScale=25，绕过原有 110 上限与 min(90,期望值) 下限；现同步为 110/90。保留 Target 子级跟随、固定逻辑区域、偏移及正文省略，不修改玩家保存值。
- 调试几何曾转换成 Target 局部坐标，但 overlay、裁剪窗口和标签都在 root，导致混用。统一四角转换到 root。保留工作区已有的取消实心填充修改，删除多余的调试 ClipMask，只保留无底色文字与线条；不改正常内容遮罩。原有修改未同步到构建哈希，本轮已重新编译并嵌入两份 Compass。
- 新增 Passenger 模式缩放及旋转/缩放父级下的调试四角、无填充回归；测试读取真实反编译的配置，不再用硬编码 70/25 掩盖活动分支。模块函数清单旧断言 44 已按现有 55 个函数同步，本轮没有新增运行时函数。
- 51/51 反编译布局/状态模拟、24/24 Python、双语、三项构建哈希及主包14/CHS1打包一致性通过。仅增量部署两份内嵌 Compass，哈希一致；DLL、用户设置、汉化及MO2状态等100个受保护文件未变。备份 `_patch_work/compass-debug-scale-20260910-012723`；未暂存或提交。
- 待验收：重启游戏，在同一任务下开关Debug，确认不再出现白色实心区域，框随内容对齐；面板期望110时effective不低于90（玩家主动设置低于90除外）。现有Passenger区域仍是逻辑限制，不能据测试宣称VR物理显示边界已标定。


## 2026-09-09 视觉残片后续
- 标题素材黑色非缩放尖角描边已改为 1px 随缩放圆角，原始素材归档并纳入可重复构建/哈希链。
- QuestList 遮罩改为排版后内容边界；顶部“◆ …”实际来源未证实，仍待实机验证。
- 18/18 模拟、构建哈希、打包通过；4 个 SWF 已备份部署且哈希一致。未动设置、Git 暂存区或 VR 网格。
- 相关说明已同步 quest-list 与 experience；下一步是重启游戏验收两处残片。

## 需求与验收目标
来源为用户反馈：检查中文和语法，修正设置绑定/帮助，随后排查菜单消失。验收需同时满足静态契约通过、Helper 成功加载、游戏菜单可见及中文显示。

## 设计与决策
- 两个缩放默认值移到 `[Display]`，保持 JSON 与 C++ 已有接口不变。
- 帮助默认值按 INI 校正为 0.008、0.125、0.675。
- 选择明确标注偏移控件“当前无效”，而非删除旧 ID；保留兼容，不宣称其生效。
- 扩展校验到 INI 分组、数值默认值、类型范围及 C++ 注册/回调；避免翻译校验假通过。

## 实现与验证
- [x] 中英文配置、生成器、默认 INI 和文档同步。
- [x] 6/6 绑定回归通过；30 个 ID、84 处文案检查、5 组选项、14 个格式串检查通过。
- [x] 主包/CHS 重打包并验证配置与母本一致。
- [x] 只读检查 MO2 Default 与 2026-09-08 22:26 启动日志。
- [ ] 查明 MCMHelper.dll 加载失败原因并修复。
- [ ] 新启动日志成功加载、菜单可见和中文显示验收。

## 阻塞与下一步
罗盘 DLL 加载正常，Helper 加载失败。已知 Helper 为 1.6.3.0，但具体根因未证实，不能仅凭版本号断言不支持 VR。核对安装包、安装选项和依赖，具体证据只维护在 [MCM 文档](../docs/mcm.md)。

## 复盘与边界
未替换 Helper、未重置菜单、未改存档；不得把配置测试通过写成故障修复。可复用经验已同步 [experience](../docs/experience.md)。

## 待更新文档
- [ ] 菜单恢复后更新 CONTEXT、mcm.md 与本阶段验收项；没有证据前保持未解决。

## 后续状态：Helper 注册恢复，名称/距离缩放修复待验收
最新检查的 22:37 启动记录中，MCM Helper 已注册 98 个设置、2 个菜单配置和 Papyrus 函数，故不再沿用“Helper DLL 加载失败”作为当前阻塞。用户保存的 Display 名称/距离缩放值均为 300，插件日志也记录 INI 变更后的重载；未修改用户保存值。

已安装 DLL 与修复前构建 DLL、已安装 VR Compass 与修复前发布 SWF 的哈希一致。SWF 动画关键帧存在容器矩阵覆盖，现把缩放移至内部文字层，避免动画与任务列表继承缩放；行为测试和重打包通过。新资源尚未部署，菜单内调节后的实际游戏效果仍待确认。这次没有更换 Helper 或修改存档。

## 后续实机反馈：缩放仍无效，面板放大被裁切
用户截图显示详情文字被固定区域截断，不是已证实的文本高度不足。最新检查：面板保存为 235%，名称 265%、距离 270%；23:12 启动及后续 INI 重载记录存在。启用模组的 DLL 与上轮构建 DLL、VR Compass 与上轮发布 SWF 的哈希一致，排除该路径下未安装上轮 SWF。

因此此前文字层缩放方案不能标记为修复成功。增加低频诊断：原生 setter 的输入和调用成功状态、SWF 方法类型/保存值/实际缩放及文本矩形，以及列表内容在 HUD 根坐标系的边界。诊断需要配套 DLL 与 SWF；在设置应用、重载和既有限次锚点日志中输出，不每帧写日志。

下一步安装诊断包，重启并修改一次名称/距离后关闭菜单，检查 `[MarkerScale]`、`[QuestListLayout]`、`[QuestListAnchor]`。在取得数据前不改存档、不恢复 UV/网格补偿，也不把纹理取样裁切猜测写成已确定根因。

## 用户确认残片来源：已完成目标行
用户明确顶部“◆ …”为下方已完成目标被截断的部分。代码检查发现行高仅取文字高度，27px 菱形和滤镜可能超出行边界。统一初建与扩宽行高为 max(文字高度, 菱形高度)+8，上下各预留 4px，文字和图标居中；整体布局与整行隐藏复用此高度。保留菱形大小及描边。该缺陷已修正，但截图中顶部呈现机制与最终消失效果仍需 VR 验证。

新增短已完成目标的菱形/描边边界回归，19/19 模拟、三项哈希及打包通过；两份内嵌 Compass 已备份部署，哈希一致。未改设置或暂存区。

## 蓝圈范围澄清与描边回退
用户圈定的是装饰上方的黑色背景区域，之前修改标题素材描边属于误解。现恢复两份 QuestItemListArt.swf 原始字节，构建仍校验素材哈希；打包并备份部署通过。已完成目标的行高/裁剪修正保留。静态检查未发现列表独立黑色背景或父级大范围阴影，截图黑区仍需同视角隐藏面板的对照画面定位，不能宣称修复。

## 黑区随面板消失：素材库背景隔离
用户确认蓝圈黑区随面板消失，UI 关联已确认。静态素材没有背景位图或 opaque placement，但原素材库保留 #333333 的 SetBackgroundColorTag。仅移除此定义并重建两份库资源；其余标签语义一致（JPEXS 对零坐标 moveBits 的编码规范化除外），不改变装饰描边。此为单变量排查，不能把舞台背景定义直接认定为导入实例黑区根因。

构建、三项哈希、主包/CHS 打包通过；两份素材库已备份部署并逐一验证哈希。保留已完成目标行高修正、用户设置与 Git 暂存内容；下一步重启游戏复查蓝圈区域。

## 2026-09-09 名称/距离上限 150% 与海拔箭头
用户已确认此前黑区来自任务/地点名称放大后的下半部分跨到顶部显示。撤回装饰素材描边及背景定义试验，恢复原始素材，不将这些试验当作根因修复。

名称和距离的 MCM 最大值、C++ 设置回调及 AS2 setter 同步限制为 150%（最小 25%、默认 100%）；面板期望上限保持 110%。不回写用户已保存值，超过范围的值在运行时限制。名称菜单改为“任务/地点名称大小”（Quest/Location Name Size），距离帮助注明同时控制海拔上下图标。

原脚本已引用 DistanceScale，但把箭头素材约 35% 的初始矩阵比例直接覆盖，导致异常放大。现缓存每个箭头实例的基础 X/Y 比例，乘以距离百分比，不累计缩放；名称更新不改变箭头大小，箭头定位仍跟随距离文本末端。没有修改海拔阈值或方向判断。

反编译行为测试覆盖名称独立性、距离 25/50/150%、超范围和无效值、连续帧更新及 Above/Below 两种状态；MCM 6 项绑定测试、完整双语/暂存配置检查、SWF 哈希、DLL 编译及主包/CHS 打包通过。MO2 主包和 CHS 的 7 个更新文件已备份部署且哈希一致。未修改用户设置、存档或 Git 暂存区。VR 视觉效果及 150% 是否完全避开采样边界仍待实机验收。

## 距离缩放偏移修复
用户反馈距离缩放后不居中。原代码仅修改距离文字 _xscale/_yscale，autoSize="center" 不能为显式缩放固定中心。现缓存初始距离中心，按文字+可见箭头+间隔的总宽度居中；箭头隐藏时仅居中文字。使用箭头实际 bounds 补偿非零注册点；更新距离时先选择上下状态/可见性，再计算布局。

反编译模拟覆盖米/英尺、3 种距离长度、上/下/隐藏状态及 5 次缩放顺序，共 90 组组合，并检查每组连续 10 帧不漂移。构建、哈希和主包/CHS 打包通过；两份 Compass.swf 已备份部署 MO2 主包，哈希一致。沿用上一轮 DLL/配置，150% 上限与名称缩放独立性保持；VR 实机效果待验收。

## 2026-09-09 偏移范围与最大高度清理完成
QuestList 横向/纵向偏移已从 -300～300 统一收紧到 **-100～100**，步长仍为 5。英文/中文 MCM JSON、`Settings.cpp` 运行时 clamp、`QuestItemList.as` 的 `SetOffsets` clamp 与布局回归边界断言同步修改，避免只限制菜单而运行时仍接受旧范围。

“最大高度”设置的活动代码已完整清理：删除 `Settings.h` 的 `maxHeight`、`Settings.cpp` 的 `fMaxHeight:QuestList` 分发与注册、两份默认 INI 的 `fMaxHeight`、`QuestItemList.h` 的 `SetMaxHeight` 封装、`QuestItemList.as` 的空兼容入口，并把构建/布局/MCM 回归改为断言该退役设置不存在。`decompiled/` 与 `tools/verify_questitemlist_vr.py` 属于历史参考/退役流程，未修改。

验证：MCM 绑定测试 7/7、QuestList 布局/状态模拟 28/28、QuestList/Compass/嵌入 SWF 检查、双语 MCM 校验和 `build.bat` 完整 DLL 构建/打包全部通过，已生成  主包及 CHS 包。**尚未重新部署到 MO2/目标机器**；部署后仍需 VR 实机验证保存/重载、±100 边界和罗盘跟随。

## 2026-09-09 任务列表 MCM 双栏与注视默认值
用户要求“显示”和“罗盘注视”左右分栏，并默认开启“需要注视罗盘”。采用 MCM Helper 原生 `cursorFillMode=topToBottom` 与标题 `position=0/1`：左侧保留偏移、面板大小、室内外显示、战斗隐藏；右侧保留全部注视开关、模式、角度、距离与掌心设置。沿用已有罗盘页的右列定位方式，不增加自定义 SWF 或改变设置 ID/业务逻辑。

核对发现 C++ 初始值、MCM 默认 INI 和双语帮助原已为开启；补齐 SKSE 默认 INI 的 `bRequireLookingAtCompass=1`，使默认文件明确表达同一契约。截图中的关闭状态不能仅凭截图归因于默认值错误，已有 `MCM/Settings` 保存值仍具有最高优先级；本轮不清理、不覆盖用户保存设置。新增双语列分组和三处默认值/原生绑定回归。

验证：双语生成 72 条替换全部命中，MCM 回归 9/9、29 个控件结构/绑定及暂存副本校验通过；`build.bat nobuild` 三项 SWF 哈希检查通过，重新生成  主包与 CHS 包。未修改 C++/SWF 源码，沿用上一轮 DLL；未重新部署或进行 VR 实机布局验收。下一步部署后检查左右列、旧保存值优先级及切换开关后的实际注视门控。

## 2026-09-09 详情装饰横向越界修复
用户截图明确指出正文、OBJECTIVES 分隔装饰和目标名均超出任务详情装饰左右，要求以装饰内部为共同边界。检查原始 SWF 后确认：`TitleTextField` 只覆盖标题装饰的中心文字间隙，原 `SetQuestInfo` 采用文字与装饰联合外框，`ReflowWidth` 又可单独扩宽内容到 1.6 倍，而标题装饰不扩宽。此前舞台边界与高度测试不能证明内容始终位于自身装饰内部。

本轮在 `QuestItem.as` 中按当前任务类型的真实 Bracket/EndPiece 几何取安全区，保留 12px 内边距及 4px 遮罩余量；正文、分隔线、目标菱形和文字共享该宽度。生成内容放入独立 `DetailContent`，使用随内容高度更新的局部遮罩，原标题文字、装饰及动画保持在遮罩外。分隔标签从原始字号重新测量，避免窄宽度下累计缩小；目标行不再有可超出内容区的 40px 最小文本宽。`ReflowWidth` 不再允许超过自然装饰内宽，保留内部窄排版的最小 48px 空间。列表自适应改为固定宽度→缩小→既有整段/整行省略，90% 下限、安全底边和偏移范围不变。

新增原始装饰尺寸、长单词、长翻译、局部遮罩、嵌套变换、窄排版恢复和禁止单独扩宽的回归，并在 SWF 构建中断言新接口及遮罩链存在。正式编译、31/31 反编译模拟、三项 SWF 哈希、MCM 9/9、双语配置和 `build.bat nobuild` 主包/CHS 打包通过。原始素材、C++、MCM 配置与 Git 暂存区未因本轮修改；未重新部署。VR 实际横向裁切、字体换行和长内容省略仍需新包实机验收，不能将模拟通过等同于画面确认。

## 2026-09-09 DetailBox 统一排版重构
用户明确要求“原始装饰决定 Box 宽度 → Box 统一排版所有内容 → 高度交给现有安全区适配 → 遮罩只处理极端绘制越界”。将上一轮仅作为生成内容父级的 `DetailContent` 改为真正的不可见 `DetailBox`，保留原标题装饰在外。每次 `SetQuestInfo` 选择任务类型后只测量一次装饰内宽和标题底边，设置 Box 的局部原点、自然宽度；使用独立 `layoutWidth/layoutHeight`，绝不以 MovieClip 的 `_width/_height` 作为布局属性，避免子元素或未裁切素材反向撑大容器。

正文、OBJECTIVES 标题/双翼和所有目标行均在 Box 的局部坐标中创建。`LayoutDetailBox` 从 y=0 开始，统一设置子元素宽度、测量换行高度并累加纵向间距；无详情/无目标不预留空区，更新任务时清理旧目标容器。分隔标签从原始字号重新测量，原有菱形尺寸、状态、排序和文字内容不变。`ReflowWidth` 仅调整 Box 的逻辑宽度并调用同一排版函数，不单独缩放文字或突破装饰。外层 `contentRect` 仍包含原装饰，Box 的逻辑高度由四角坐标转换交给现有安全底边、自动缩小与整段/整行省略；不使用 MovieClip 子节点的原始绘制外框作为高度。局部遮罩随 Box 的宽高更新，留 4px 滤镜余量，正常排版不依赖遮罩裁切。

验证：正式 SWF 编译、34/34 反编译布局/状态模拟、三项 SWF 哈希、MCM 9/9、双语配置及 `build.bat nobuild` 主包/CHS 打包均通过。新增 Box 局部原点/尺寸所有权、空区、窄宽恢复、超大导入素材、嵌套旋转缩放及高度隐藏恢复测试。首次检查发现 Compass 基线清单过期，已按当前源码重建基线并重新嵌入，最终三项哈希全部一致。没有修改原始素材、C++、MCM 默认值或 Git 暂存区，也未重新部署；VR 字体实际换行、遮罩边缘和省略恢复仍需实机验收。

## 2026-09-09 12:44 MO2 部署
用户明确要求部署，已核对 Default 配置中主包和 CHS 均启用，SkyrimVR 未运行，并在写入前复核三项 SWF 哈希和双语配置。以本轮  主包与 CHS 包为来源，跳过用户 SKSE INI，不清理模组目录，也不修改 MO2 优先级或启用状态。13 个发布文件中 9 个原已一致，实际更新两份嵌入式 Compass.swf 和主包/CHS 的 MCM config.json。覆盖前备份至 `_patch_work/compass-deploy-20260909-124424`，manifest 记录原始/新 SHA-256、文件存在状态和保留文件哈希；采用同目录临时文件替换，并在失败时回滚已写入项。

部署后 13/13 发布文件与 ZIP 逐一哈希一致，6 个用户/配置文件哈希未变（包括插件 INI、两份 MCM 保存 INI 和 Default 的 modlist/plugins/loadorder）。未部署独立 QuestItemList.swf，未改 Git 暂存区。此结果证明目标文件已安装，不等于 VR 已加载或视觉修复已验收；下一步重启游戏检查正文/分隔线/目标行的装饰内缘、长文本及省略恢复，同时验收 MCM 双栏、默认值保存优先级和 ±100 偏移跟随。

## 2026-09-09 Header 纳入完整布局
用户进一步要求将原版 TitleBracket、TitleEndPiece、TitleTextField 也纳入 DetailBox，而非删除其视觉功能。原版 QuestItem Sprite 6 的18帧时间轴直接动画深度1的Title（Sprite 5），AS2不能像浏览器一样安全地直接重设原有时间轴实例的父级。因此在构建期由`tools/questlist_header_layout.py`增加一帧包装Sprite 306：原Title整体成为Header，原深度1放置改为DetailBox，保留其矩阵、alpha、混合方式及全部后续动画帧。原Title符号定义、任务图案、杂项标题和侧边图案不改、不复制或删除；构造函数通过`DetailBox.Header`保存原Title兼容引用。

现在完整层级为`QuestItem → DetailBox → Header / DetailBody / DetailContentMask`，Body内为正文、OBJECTIVES和目标容器。外层Box拥有完整装饰逻辑宽高，Header保持原局部坐标，Body从装饰底边加4px开始，使用原有12px内边距；`LayoutDetailBox`汇总Header与Body高度，`GetContentBounds`、渲染底边和高度裁切共享逻辑矩形及四角变换。`ReflowWidth`只调整Body内宽，原Header不随文字单独扩宽；局部遮罩仅作用Body。原有90%缩小下限、110%期望上限、整段/整行省略、偏移与注视门控不变。

验证：新增原始/生成SWF结构对比，确认Header符号及所有非脚本动画标签保持一致，包装器冲突与意外修改均能被测试拒绝。35/35真实SWF反编译布局/状态模拟、3/3包装器单元测试、MCM 9/9、双语配置、三项SWF哈希及两份安装包校验通过。Compass基线因清单过期按当前源码重建后重新嵌入，未修改C++或原始素材，沿用既有DLL。此次未覆盖12:44已安装构建，也未修改用户配置、MO2状态或Git暂存区；新包需部署后VR验证完整动画、字形边缘和省略恢复。

## 2026-09-09 13:09 最新统一布局部署
用户要求部署（称MO3，按本对话既有MO2 Default目标执行）。写入前确认SkyrimVR未运行、主包及CHS已启用、源代码包含Header包装引用，三项SWF哈希及双语配置检查通过。最新主包/CHS共13个非用户配置发布文件，11个已与安装一致；仅更新HUDMenu和VR_HMD_info的嵌入式Compass.swf。未单独安装QuestItemList.swf，未修改MO2优先级、启用状态或任何用户设置。

覆盖前备份`_patch_work/compass-deploy-20260909-130942`，记录原始与新SHA-256及发布包哈希；同目录临时文件替换，失败时可回滚。部署后13/13文件与包逐一哈希一致，插件INI、两份MCM保存文件及Default配置等86个保留文件哈希未变。最新Header统一布局已安装，但尚未启动VR验证实际加载、完整动画、字形边缘与省略恢复；Git暂存区未改。

## 2026-09-09 提前省略诊断与修正
用户实机截图显示完整任务描述下方仅有`. . .`，目标段落没有出现。检查确认该符号来自`PrepareOverflowLabel`主动省略，而非局部遮罩。已找到可复现的代码缺陷：部分高度路径把全局Y当作根坐标Y转换，缓存命中时`fitOverflow`可直接阻止后续几何变化的重新适配，旧省略流程仅以完整外框判断并先预留标签空间，空Body的额外余量也可能被当成缺失内容。截图对应的实际Stage尺寸、父级变换和VR贴图可视边界没有可用运行时日志，因此不能断言这些缺陷已解释全部画面现象。

`QuestItem.as`新增按指定坐标空间获取完整底边、Header底边、最后可见内容底边及真实省略状态；`ApplyHeightLimit`保留原有单参数全局接口，并可接受根坐标空间。`QuestItemList.as`统一使用根坐标进行适配、裁切与省略，缓存记录最终几何及时间轴帧，发生变化时重新求解。先检查哪些完整段落/行真正放不下，再预留省略号；省略号紧随最后可见内容，若预留会挤掉原本可读的描述或标签自身放不下，则保留原内容而不强行显示省略号。标题必须完整位于安全区内才显示，原整行隐藏及恢复规则保留。安全底边仍为`Stage.height - max(20, Stage.height*0.08)`，额外8px安全余量、110%期望上限、min(90,期望)缩小下限、固定装饰内宽、偏移与注视门控均未放宽。

新增`GetLayoutSnapshot`按需刷新一次布局并返回详细诊断，`MessageListeners.cpp`在设置应用/实例初始化时优先调用该方法，兼容旧`GetAnchorState`。诊断包含根坐标安全/内容限制、完整与可见底边、Header/Body逻辑尺寸、每行可见状态、缩小/省略原因；不在普通帧循环中写日志。正式SWF重建并嵌入两份Compass，41/41反编译布局/状态模拟、3/3包装器单测、MCM9/9、双语配置、三项SWF哈希、完整C++ DLL编译及两份 ZIP完整性/内容校验通过。仅编译时出现已有的SE/AE宏覆盖警告；没有修改MCM/INI默认值、原始素材或Git暂存区。**新包尚未部署到MO2，VR实机效果及实际安全边界仍待复测。**

## 2026-09-09 布局可视化调试
用户要求调试模式直接显示红框、尺寸和坐标。复用现有`uLogLevel:Debug`：Trace(0)/Debug(1)启用，Info(2)及以上关闭；新增`bShowQuestListLayout:Debug`默认1作为独立禁用项，最终生效须同时满足日志级别与开关。通过原三层INI注册/分发，设置应用和实例创建时调用兼容检查后的`SetLayoutDebug`。MCM仅更新日志级别的中英文帮助，不改变29个控件或任务列表双栏；用户已有INI/MCM保存值不覆盖。

`QuestItem.GetLayoutDebugRegions`只读返回完整Box、Header、Body、Body mask、原版Bracket/EndPiece、杂项标题、正文、OBJECTIVES和最多24个目标行的逻辑矩形及显示状态。`QuestItemList`将四角转换到根坐标，在`_root`独立创建调试层，绘制红色布局框、橙色标题框、青色遮罩/限制线及隐藏行虚线；每个区域标注根坐标AABB与局部宽高。摘要显示Stage、请求/生效缩放、安全/内容限制、完整/可见底边、目标可见数和适配/省略原因。实际安全底边与额外8px限制保持原值，不推测VR贴图UV。调试层不在DetailBox或viewportMask内，不改变任何布局测量、原始资源或可见性；按几何签名复用文字标签，仅调试开启且列表可见时更新，关闭/卸载清理，最多采样8个任务、每项24行。

新增4项可视化回归及1项设置回归，覆盖默认关闭、独立层不反向影响布局/缓存、旋转缩放四角、实际安全线、隐藏行恢复、门控、标签复用/上限和卸载清理。45/45正式SWF反编译布局模拟、3/3包装器、MCM10/10通过，DLL完整重编、两份Compass嵌入及主包/CHS生成成功。双语校验对配置标识符采用精确白名单，不放宽普通译文校验。**本轮不部署MO2；实际字体尺寸、红框可见性及VR坐标映射需要新包部署后实机验收。**

## 2026-09-09 QuestList 源码模块化整理
用户要求拆分、整理代码，并允许按需新增文件夹。选择保留现有AS2链接类和时间轴入口，在构建期组合源码，不引入新的运行时继承链、独立SWF或加载边界。`swf/QuestItem.as`保留类声明与字段，`swf/QuestItemList.as`保留时间轴状态、全局初始化和`stop()`；实现迁入`swf/questlist/item/`七个模块（Initialization、Artwork、Content、Layout、Geometry、Debug、Lifecycle）及`list/`六个模块（Core、Entries、Layout、Overflow、Diagnostics、Debug）。源码文件使用明确的`// @include`指令，`tools/questlist_source.py`验证并组合到原两个编译入口；JPEXS、原版Header包装器、六参数接口及原18帧动画不变。没有修改运行时排版、缩放、省略或调试规则。

构建工具把所有模块和组合器纳入动态哈希清单，拒绝缺失、重复、循环、越界和孤立引用及重复函数/意外类。原近千行Node测试拆为共享模拟环境和item/layout/debug三组，保留原测试入口与45个用例；新增10项Python模块回归，检查原入口API、模块完整性及哈希覆盖。拆分前冻结源码与反编译基线于忽略的`build/questlist-refactor-baseline/`；逐函数比对确认QuestItem31个、QuestItemList40个函数体均未改动。测试迁移时曾因按文本`});`截取而截断嵌套回调，已依据保留片段及原用例恢复完整语法块、核对45个用例并全数复测通过，未把错误片段纳入发布。

正式构建从原始素材重新生成QuestList，Compass基线因构建脚本哈希变化按当前源码重建后重新嵌入两份Compass。10/10源码模块、45/45真实SWF反编译布局、3/3包装器、MCM10/10、双语配置及三项SWF哈希检查通过；`build.bat`配置、编译和打包成功，C++无本轮修改，Ninja报告无需重新编译。27项QuestList清单全部匹配；主包/CHS ZIP完整性、对应中英文配置、DLL、插件INI及嵌入SWF逐项校验通过，没有独立QuestItemList.swf。主包SHA-256：`16b94d95cc569e6260497d8590c0436e4fd6a1e5766d1b06d9a82aa1b52a5673`；CHS：`eef9caa8b2c69f64641e2c0bb2731b643395d8daccf035f6125c0bcaad727b3d`。

源码目录说明、编辑入口和测试命令已更新到`swf/questlist/README.md`、`tools/README.md`、QuestList专项文档及开发手册。**未部署MO2、未运行VR；用户保存配置、原始素材、`decompiled/`、Git暂存区均未修改。**下一步仍需在同一任务复现提前省略，采集红框与运行时快照，再判断VR安全边界是否需要调整。

## 2026-09-09 C++ 与资源拆分收尾
用户要求拆分整理 C++ 和资源，并检查其他整理项。接手时工作区已存在 HUD、QuestList、设置和 UI 的未提交拆分，Hooks 公共头已精简但私有实现尚未落地。依据冻结基线补齐 source/hooks 下安装、任务匹配、普通标记、效果转发、兼容实现及 Internal.h；Compass 的11个方法移入 source/ui/Compass.cpp。首次完整编译发现旧拆分缺少 NND 头文件与设置私有头路径错误，均按实际报错修正。10个回调、2个安装函数和11个Compass方法与基线函数体一致（忽略空白），地址偏移、调用顺序、诊断及原行为保留。

资源按主包、中文覆盖、原始素材和生成中间件分为 assets/main、localization/zh-CN、source/questlist、generated。中文 config.json 保持游戏覆盖路径；工具、翻译、嵌入和哈希路径全部同步。8个非文档资源字节保持不变，两份发布Compass重建后也与原版一致。新增包内容校验器检查缺失、多余、重复ZIP条目及哈希；CMake纳入私有头并修正两行坏UTF-8注释，BAT保持CRLF。第一次打包因迁移脚本误写LF导致CMD截断命令，确认换行原因后恢复CRLF，完整build.bat复测成功。

DLL编译/链接及主包、CHS生成通过；Python23/23、正式SWF反编译布局45/45、罗盘缩放/箭头/边界、双语配置、三项SWF清单全部通过。主包13文件与中文包1文件的暂存目录和ZIP逐一匹配，没有独立QuestItemList。仅保留既有SE/AE宏覆盖警告；未部署MO2、未运行VR、未改用户保存值或Git暂存区。

目录导航、架构、资源/工具/开发指南及中英文README已更新；旧L1原文保存为docs/context-history-20260909.md，CONTEXT收拢至80行以内。后续建议：本机构建配置与打包逻辑分离、GFx同名包装器逐接口审计、当前指南和实验历史进一步分开；详见docs/organization.md。当前阶段仍以提前省略、调试红框与VR实机验收为剩余工作。待更新文档：本次相关文档已同步。

## 2026-09-09 红框不可见复查与增量部署
用户反馈Debug模式仍不显示红框。检查MO2 Default启用关系、已安装两份Compass及DLL与当时发布包哈希，确认安装文件本身并未落后；MCM保存的`uLogLevel=1`、默认独立开关1。未找到该画面对应的最新运行时日志，实际分发和VR映射仍不能据此断言。源码确认旧UpdateLayoutDebug把普通HUD的父级可见性、alpha、空列表和锚点状态当作调试层自身的显示门控，存在使诊断无法解释自身消失的缺陷。

只修改AS2诊断模块：开启时立即更新独立根级叠加层，普通HUD隐藏或空列表时仍保留摘要和已有几何，不改变实际QuestItem、遮罩、动画、适配与省略逻辑。新增GetLayoutDebugVisibility/GetLayoutDebugState报告隐藏原因、开关与实例状态；GetLayoutSnapshot附带debugState。更新旧门控预期并新增空列表/无锚点、隐藏父级与罗盘、布局不受调试影响等回归。47/47反编译布局模拟、10/10模块、3/3包装器、MCM10/10、双语及三项SWF哈希检查通过；build.bat nobuild沿用此前已完整编译的DLL，主包13文件/CHS1文件与ZIP逐一校验通过。原始素材、C++、INI默认值及MCM配置未改。

确认游戏进程未运行、两份目标SWF为唯一启用覆盖来源且其他11项主包文件已匹配后，备份并增量更新MO2 Default主包的HUDMenu及VR_HMD_info两份Compass.swf。备份`_patch_work/compass-deploy-20260909-154123`含旧文件与回滚清单；新SWF SHA-256均为`fca8403262a6d886868023235072009a3ac7cbda4512ec14382d7ffe25c77d5f`，13/13主包、CHS及保留配置和MO2状态校验通过。没有单独部署QuestItemList.swf。**安装完成，VR实机未验收。**下一步重启游戏，观察独立摘要和红框；若仍不可见，采集新日志的debugState与同画面截图，不盲目调整安全区或UV。

## 2026-09-09 原生调试开关启动同步修复
再次收到红框不可见反馈后，改用Windows User Shell Folders读取实际重定向的文档目录，取得15:47–15:51的最新SKSEVR插件日志。真实记录证实三层INI成功加载、嵌入式QuestList初始化成功，任务可见且MCM保存uLogLevel=1，但多个快照均为`layoutDebug=false; debugState=[enabled=false; overlay=false]`。这排除了此前假设的“已创建红框被渲染裁切”作为该次现象的直接原因。源码比对确认嵌入式Compass初始化分支只执行UpdateLayout/SetTextScale，漏掉SetLayoutDebug；独立QuestItemList补丁分支虽有该调用，正常VR嵌入流程不经过它。

新增CNO::UI::ApplyQuestListSettings统一首次嵌入、兼容补丁与INI热重载的设置同步，保留位置/缩放调用及原接口，调用SetLayoutDebug并读取GetLayoutDebugState。新增原生`[QuestListDebug]`请求/方法/状态日志，回归明确检查实际嵌入初始化分支。C++完整build.bat编译与链接成功；MCM10/10、模块10/10、布局47/47、包装器3/3、罗盘回归、双语和三项SWF哈希通过，主包13项/CHS1项与ZIP一致。仅原生DLL改变，SWF和原始素材未改。

确认游戏未运行、无其他已启用DLL覆盖、其余12项主包文件已与新ZIP相同后，备份并原子替换MO2 Default主包DLL。备份`_patch_work/compass-deploy-20260909-155751`，新DLL SHA-256为`86dc6835c01b94d6b8958909a9d62058145776de1514618333849cd2f627fe55`；13/13主包、CHS及27项保留配置/模组状态文件哈希验证通过，优先级、用户INI和存档未修改。**安装已验证，VR实机效果仍待验收。**下次启动先确认requested/method/enabled均为true，再根据截图与实际日志判断是否仍有渲染问题。

## 2026-09-09 UTF-8 BOM 首节读取修复
16:03–16:05再次复测的真实日志显示新原生同步已生效、SWF方法存在，但requested=false，用户保存的uLogLevel=1没有成为有效值。检查实际重定向文档目录中的最新日志及MO2覆盖来源后，确认默认INI为Info(2)、保存INI为Debug(1)。原始保存文件以EF BB BF开头，[Debug]为首节；直接调用Windows配置API复现Debug键缺失，而后续QuestList键正常。对临时去BOM副本读取可得到1，定位为原生INI解析的编码兼容问题，不是用户未保存或VR绘制裁切。

新增include/utils/INIInput.h与source/utils/INIInput.cpp，在检测到UTF-8 BOM时以临时去BOM视图供原有INISettingCollection读取，结束即删除临时文件；无BOM文件原样透传，三层优先级、注册/分发、所有用户配置及原始素材不变。补充有效日志级别与独立开关诊断。原生MSVC/Win32回归4/4，MCM11/11、模块10/10、真实SWF布局47/47、包装器3/3、罗盘回归、双语与三项SWF哈希通过；完整build.bat编译链接及主包13/CHS1校验通过。仅DLL改变，未重编或独立部署QuestItemList.swf。

确认游戏未运行、无其他启用DLL覆盖且其他12项主包文件与发布ZIP一致后，备份并原子替换MO2 Default主包DLL。备份`_patch_work/compass-deploy-20260909-161510`，新DLL SHA-256为`eccc7fa7c9ff2f10c8057a3b055acb3c116806e3e5b0731b1f198d95f6d2c6bd`；13/13主包、CHS及27项受保护配置/模组状态文件哈希通过，用户INI、优先级与存档未修改。**安装完成，VR实机未验收。**下一步重启游戏，先确认有效level=1、allowed=true、requested=true、method=true、enabled=true，再根据红框与实际安全边界判断提前省略。

## 2026-09-09 Debug 彩色边框绘制修复
18:52–18:54最新运行日志确认有效level=1、allowed/requested/method/enabled/overlayVisible均为true，任务存在时hudVisible=true；此次不是BOM或启动同步失败。工作区的AddHudReferenceDebugRegions调用anchorText.getBounds(_root)，但真实AS2 TextField不提供MovieClip的getBounds/localToGlobal。新增模拟用例移除这三个错误API，旧正式SWF明确在anchorText.getBounds处失败，暴露现有模型继承MovieClip而漏报的问题。

改为获取名称字段在父容器中的矩形，通过父MovieClip转换四个角点；旋转、缩放和根坐标均在测试中检查。统一DebugMode与layoutDebugEnabled两套状态，避免日志显示false而绘制开关为true；增加drawnRegions诊断，绘制成功后才写缓存签名。原生头文件中缺失的独立开关和统一判定接口补齐，构造/设置应用遵循同一门控，删除UpdateLayout中重复启用导致每次清空缓存的调用。保持用户当前Stage.height视口边界、动画、布局、隐藏文字摘要的设置及全部保存配置，未恢复旧保护带。

测试模型的罗盘参考改为真实MovieClip模拟，新增TextField无MovieClip方法、有限角点和实际彩色画线验证；旧测试中已退役的GetSafeBottom引用改为当前GetViewportBottom契约，省略夹具随当前边界调整。正式SWF反编译布局48/48、Python24/24、双语配置、三项SWF哈希和主包13/CHS1文件及ZIP校验全部通过。DLL完整编译链接成功，原有SE/AE宏覆盖警告保留。

游戏未运行且无其他已启用覆盖来源时，备份并更新MO2 Default主包DLL和HUDMenu/VR_HMD_info两份Compass。19:02:55部署备份为_patch_work/compass-debug-borders-20260909-190255；主包13/13及CHS逐项匹配，27项受保护用户配置/模组状态文件未变。首次部署准备阶段遇到PowerShell将File.Replace的null备份参数绑定为空路径，未替换文件；改用明确的原子备份路径后部署成功。未单独部署QuestItemList.swf、未修改Git暂存区。代码与安装已验证，仍需重启SKSEVR实机检查颜色边框以及drawnRegions大于0。
## 2026-09-09 19:06 实机截图与边界复核
用户截图已确认彩色边框实际可见，但QuestList底部截断且上方出现分离的红框。19:06–19:08同次运行日志显示drawnRegions=13、任务三行均visible=true、omissionReason=none；完整底边1023.944、第三行文字994.625–1017.131，当前QuestList下限1024。这表明AS2认为内容完整容纳，不能把截图底部消失解释为本轮代码主动省略。

调试线位于根级独立层，绘制完整逻辑矩形，不继承QuestList/Body遮罩；超出实际显示区域的框仍会进入VR贴图渲染。分离红框与VR显示映射/分区导致的内容分裂相符，但截图无区域编号，不能确认具体是哪一行，也尚未测定实际贴图截止坐标。绿色是Compass包围框、黄色是名称TextField包围框，不是上下安全区。

当前边界存在两套规则：QuestList适配顶部20、底部Stage.height=1024，已无原8%底部保护带；遮罩顶部另有12像素限位。名称字段仍由ApplyNameViewportGuard按top>=12、bottom+4<=942.08控制，仅名称保留8%保护。名称保护不能解释QuestList底部截图裁切。应区分SWF逻辑坐标与VR实际显示范围，下一步需带坐标标尺/区域标识采样，才能据实设定上下边界。本轮只复核并记录，未修改运行代码或重新部署。

## 完整构建与全包部署确认
2026-09-10 01:30按用户要求执行完整build.bat（DLL已最新，Ninja无需重编），SWF哈希及主包14/CHS1打包检查通过。部署14文件哈希一致，保留用户SKSE INI及88个受保护文件；备份`_patch_work/compass-full-deploy-20260910-013006`。未暂存/提交，VR复测待用户确认。
## 距离缩放遮挡罗盘标记

- 用户确认：放大距离后遮住的是下方地点/任务标记，不是距离旁的海拔箭头。
- 原因证据：AS2原实现只处理缩放和横向居中，没有纵向锚点；SWF素材中Distance位于标记上方。新增测试在旧版150%下复现文字底边26→38，说明文字向标记方向扩展；这是模拟证据，不是实机像素测量。
- 修复：`swf/CompassMarkerInfo.as`缓存距离字段原底边，缩放后按原底边重新定位Y；海拔箭头缓存原Y，并按当前Above/Below帧的缩放后边界换算原底边，不累积补偿。保留原横向组合居中、100%位置、时间轴父级矩阵、名称及QuestList设置。
- 验证：旧版新增测试先失败，修复后预检和最终嵌入SWF均通过84组底边/反复缩放/时间轴回归；原有尺度/居中/名称边界测试继续通过。既有QuestList56项、Python24项和主包18/中文1文件与ZIP检查通过。
- 构建：重编Compass基底并嵌入两份发布SWF，更新相应哈希清单；`build.bat nobuild`只重新打包，不改变上一轮原生重构的部署状态。
- 部署：用户确认后仅替换现有主包两份Compass.swf；114个保护文件（含DLL、Palm、配置、CHS）哈希不变。备份`_patch_work/compass-distance-anchor-20260910-102121`。没有部署上一轮重构DLL，没有Git提交。
- 待验收：重启后比较100%/125%/150%、m/ft、上下海拔状态；标记不应被文字/箭头下扩遮住。上方共享显示区余量、真实字体滤镜和所有姿态仍需实机确认；不把保持逻辑底边称为全姿态无遮挡证明。

## 2026-09-10 普通 HUD Debug 重写
用户批准彩色空心边框、短 ID、最多两行中文与详细日志，要求替换项目原有普通 Debug，而非重写正常 HUD。删除旧红框／坐标表／逐行屏幕标签与父级隐藏绕过；Compass 时间轴拥有独立观察器，QuestList 只提供读数据，十字校准拆至 Calibration.as。bDebugOverlay 默认关闭，设置热重载与原生有界日志链路补齐；MCM 双语帮助不再声称日志级别自动启用框。

构建时读取真实 FadeIn/IdleShow/FadeOut 帧标签及 Distance 静止矩阵；参考仅代表原动画矩阵加当前祖先变换，不是实测世界／UV边界。线框每帧、描述和动画采样约5Hz、状态变化即时入队；稳定无变化不刷日志，队列和快照均有上限。完整定义见 [HUD Debug](../docs/hud-debug.md)。

最终嵌入式 SWF：49项布局／校准、9组新观察器、84组距离底边及既有接口回归通过；24项Python、完整DLL编译、主包18／CHS1及ZIP一致性通过。旧的“即使父级隐藏也显示”测试被有意替换，不把已废弃语义当作兼容目标。没有改正常 QuestList 布局、名称／距离动画、原始装饰、Palm或安装配置。已安装文件未改，未暂存／提交／推送；本次构建产物尚未部署，需另行授权，不能沿用距离修复的两SWF部署许可。VR显示、字体、重建与映射仍须实机验证。

本节之前的旧普通 Debug 开关、红框和坐标摘要仅为历史记录，现行方案以本节及专项文档为准。待更新文档：已同步索引、架构、QuestList说明、使用手册及经验。

## 2026-09-10 普通诊断统一由 MCM 日志级别控制
用户明确要求 Debug／Trace 同时启用新方案全部普通诊断。已移除未部署的 bDebugOverlay 独立设置及注册，原生统一使用 IsDebugEnabled；Info 及以上关闭，发布默认 Info。MCM 双语帮助、默认 INI、回归与当前指南同步。边界校准仍独立，避免普通诊断自动隐藏任务列表。此决策取代上一节的独立普通开关设计，未恢复旧红框绘制。构建后仍须另行确认部署。

## 2026-09-10 审查发现的三项 Debug 缺陷修复
用户授权修复：多边形裁剪返回重复端点导致进入边界的线段消失；Q 借用 F 边界混入隐藏条目；空列表误报实例不存在。分别改用独立线段裁剪、可见集合独立求并集、空内容与实例／接口存在性分离；V 在空列表时继续提供逻辑视口。正常布局／距离源码未改，不恢复旧 Debug。
最终嵌入 SWF 的观察器回归增至12组，新增四边／反向／绘制端点、隐藏条目和空列表反例；49项布局／校准及84组距离回归通过。现有原生 DLL 无新修改，SWF重建后与对应DLL重新打包。未部署、未暂存／提交，VR待验收。待更新文档：本轮已同步 HUD Debug、QuestList、手册与经验，文档整体审查见 [S3](s3-documentation.md)。